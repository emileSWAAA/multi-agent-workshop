{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "tags": {
            "value": {
                "project": "Multi-Agent Workshop",
                "environment": "demo"
            }
        },
        "environmentName": {
            "value": "${AZURE_ENV_NAME}"
        },
        "myPrincipalId": {
            "value": "${MY_USER_ID}"
        }
    }
}